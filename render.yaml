services:
  - type: web
    name: women-safety-backend
    env: python
    buildCommand: ./render-build.sh
    startCommand: gunicorn wsgi:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_PATH
        value: /var/lib/data/women_safety.db
      - key: UPLOAD_FOLDER
        value: /var/lib/data/uploads
    disk:
      name: data
      mountPath: /var/lib/data
      sizeGB: 1
